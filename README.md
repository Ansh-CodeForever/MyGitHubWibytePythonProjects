# Welcome Message💕🤗✨!

We are very happy to have you here with us, we want to wellcome you. Please read the given instrunctions below.

____________________________________________________________________________________________________________________
## Short Code!

Use the function short_code to create a short code about yourself! Enter your idx, a few words about your self, your suffix and prefix. Be creative, (but not too creative).

```python
sc = short_form(idx, a few words about yourself, prefix =, suffix = )
```
(e.g.)

```python
sc = short_form(0, 'Smart', 'Intelligent', prefix = 'Mr.', suffix = 'Great')✅
```
remember when writing a word to put the speech marks
```python
sc = short_form(0, Smart', 'Intelligent, prefix = 'Mr.', suffix = 'Great')❌
```
(This is wrong, because they are missing a speech mark before 'smart' and after 'intelligent')

Note - we don't check if the words you are saying are real or not

## Name strength💪

Use the function name_strength to check the strength of your name. Enter your name, surname, aka, alias and salutation. We like creativity, we geniually like creative akas, alias!
```python
ns = ns(name = 'Ansh', surname = 'Gupta', aka = 'anchoa', alias = 'anchica', salutation = 'Mr.')✅
```
Let's say you don't have an aka or an alias or none of them you don't have to put them (e.g.).
```python
ns = ns(name = 'Ansh', surname = 'Gupta', salutation = 'Mr.')✅
```

## Tell me a word

Use the function tell_me_a_word to check how good you are at thinking words. It will ask you tell a word on a certain topic and you give an answer. Think carefully and be creative!

Step 1:
```python
tw = tell_me_a_word()
```
Step 2:
(you will see this)
```python
Tell me a ? word: (enter your answer)
```

## Guess the Number

Use the function guess_number put 2 numbers and then the computer will choose a random number between them. How many attempts will you take?

step 1 (e.g.)
```python
gn = guess_number(1, 100)
```
I want the computer to choose a number between 1 and 100

step 2 (e.g.)
```python
What is your guess
(enter your guess)
```
Every 4 attempts you will see a comment

## Validate☑️
Once you have done these steps correctly to validate your inputs do this step (e.g.):

```python
validate(sc = sc, ns = ns, tw = tw, gn = gn)
```
If your validation goes correctly your inputs will be sent for the deep validation

## Deep Validation☑️✅

For the deep validation you don't have to do anything, but you will be told when your inputs have been sent for the deep validation. Then it will try to conect to the server to see weather you have conected or not! Good Luck and Have Fun!
____________________________________________________________________________________________________________________

### Most Important

The most important things for us to tell you is some of the things may not be true, such as the name strength, don't worry if you don't get conected to the server and most important HAVE FUN AND ENJOY!🤣🤩
